{
  "frontend/src/index.css": "Global styles with a dark-mode OKLCH color system featuring deep charcoal background, teal and amber accents, glassmorphism utilities, custom font imports (Inter + Space Grotesk), and animation helpers.",
  "frontend/tailwind.config.js": "Extended Tailwind config with custom teal/amber color tokens, Space Grotesk display font, additional border-radius and box-shadow utilities, and fade-in/pulse-glow keyframe animations.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for fetching all entries, featured entries, entries by category, admin status, and categories — updated to throw errors instead of returning empty arrays when actor is unavailable (so React Query properly retries), with staleTime 0 and retry 2 for data freshness.",
  "frontend/src/components/HeroBanner.tsx": "Hero banner component displaying the generated background image with a gradient overlay, AI Hub logo, headline with gradient text, tagline, and animated decorative dots.",
  "frontend/src/components/SearchInput.tsx": "Controlled search input with a teal search icon, clear button, glassmorphism styling, and teal focus ring for real-time filtering.",
  "frontend/src/components/FilterBar.tsx": "Horizontal filter bar with category buttons (All, Image, Text, Audio, Video, Productivity), active teal gradient state, count badges, and hover effects.",
  "frontend/src/components/ToolCard.tsx": "Clickable AI tool card that accepts an `isAdmin` prop (instead of calling `useIsAdmin()` internally) and conditionally renders a hover-revealed trash button for admin users; clicking it opens an AlertDialog confirmation before calling `useRemoveEntry`, with inline error display and loading spinner during removal.",
  "frontend/src/components/FeaturedSection.tsx": "Conditionally rendered featured tools section with a sparkle header, amber gradient divider line, and a 3-column responsive grid of featured `ToolCard`s — now accepts and passes an `isAdmin` prop to each card.",
  "frontend/src/components/ToolGrid.tsx": "Responsive 4-column tool grid that accepts and passes an `isAdmin` prop down to each `ToolCard`, with skeleton loading placeholders and an empty-state illustration when no results match the current filter/search.",
  "frontend/src/pages/CatalogPage.tsx": "Main catalog page that renders HeroBanner, then FeaturedSection (only when featured entries exist or are loading), followed by SearchInput/admin controls, FilterBar, and ToolGrid — all entries (including featured ones) are shown in the main grid with counts based on all entries.",
  "frontend/src/App.tsx": "Root app component with sticky glassmorphism header including the AI Hub logo and LoginButton, CatalogPage as main content, and footer with dynamic year and caffeine.ai attribution.",
  "frontend/src/hooks/useMutations.ts": "React Query mutation hooks for adding entries, adding categories, removing categories (useRemoveCategory), and removing entries (useRemoveEntry), each with automatic cache invalidation on success.",
  "frontend/src/components/AddToolModal.tsx": "Modal dialog for submitting new AI tools with a dynamic category dropdown populated from the backend via useGetCategories, replacing the previous hardcoded category list.",
  "frontend/src/components/LoginButton.tsx": "Login/logout button component using Internet Identity; shows a teal \"Login\" button when unauthenticated, and when authenticated displays a truncated principal with a chevron toggle that expands a panel showing the full principal ID with a one-click copy button and a hint to share it with the admin for access — plus a \"Logout\" button that clears the query cache.",
  "frontend/src/components/ManageCategoriesModal.tsx": "Admin modal for managing categories — lists all categories with a trash/remove button on non-default ones (default categories show a \"default\" badge instead), includes an AlertDialog confirmation before removal, inline error display, and the existing add-category form."
}